{
  "id": "eval-04-no-secrets",
  "description": "Refactor hardcoded credentials to use environment variables",
  "category": "security",
  "pass_threshold": 90,
  "task": "Refactor this code to load credentials from environment variables instead of hardcoding them. Use os.environ.get() with a sensible default of None.",
  "starter_code": "def connect():\n    db_password = 'super_secret_123'\n    api_key = 'sk-abc123xyz'\n    return Database(password=db_password, key=api_key)",
  "assertions": [
    {"type": "contains",     "value": "os.environ",       "description": "Uses os.environ"},
    {"type": "not_contains", "value": "super_secret_123", "description": "No hardcoded password"},
    {"type": "not_contains", "value": "sk-abc123xyz",     "description": "No hardcoded API key"},
    {"type": "contains",     "value": "import os",        "description": "Imports os module"},
    {"type": "ruff_passes",  "value": "",                 "description": "Passes ruff lint"}
  ]
}
